apiVersion: v1
kind: Pod
metadata: 
  name: yolo-pod-jacket
spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: nvidia.com/gpu.product
            operator: In
            values:
            - NVIDIA-GeForce-RTX-3090
  containers:
  - name: gpu-container
    image: gitlab-registry.nrp-nautilus.io/udembys/yolo
    command: ["/bin/bash", "-c"]
    args: ["cd /home;
            git clone https://github.com/jacketdembys/iksolver.git;
            cd iksolver/;
            wandb login --cloud d7f81da19c5965b1c5eff37a677caab3ffb5379c;
            python create_experiments.py --layers 10 --neurons 1024 --scale 10 --load cloud;
            python ik-solver.py --config-path train.yaml; 
            sleep infinity"]
    resources:
      limits:
        memory: 32Gi
        cpu: 8
        nvidia.com/gpu: 1
      requests:
        memory: 32Gi
        cpu: 8
        nvidia.com/gpu: 1